import { FC } from "react";
import { ControlPanel } from "./main/control-panel";

const GameView: FC = () => {
    return (
        <ControlPanel>

            
        </ControlPanel>
    );
};

export default GameView;
